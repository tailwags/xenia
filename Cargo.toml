[package]
name = "xenia"
version = "0.0.1"
edition = "2021"
description = "xenia"
license = "Apache-2.0 WITH LLVM-exception"

[dependencies]
linux-raw-sys = { version = "0.6.4", default-features = false, features = ["general", "system", "no_std"] }
